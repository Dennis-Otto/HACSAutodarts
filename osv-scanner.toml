# Advisories that OSV-Scanner (and the OpenSSF Scorecard) should not report.
#
# Home Assistant 2026.9.3 pins cryptography 48.0.1 exactly. Only the test
# environment installs it (requirements-test.txt); the integration itself ships
# no Python dependencies. Remove these entries once the Home Assistant test base
# uses cryptography 50 or newer; dependency-review.yml lists the same advisories.

[[IgnoredVulns]]
id = "GHSA-g6cj-pr64-35w5"
reason = "Test-only dependency pinned by Home Assistant 2026.9.3; not shipped with the integration."

[[IgnoredVulns]]
id = "GHSA-jwv3-5hgf-82ww"
reason = "Test-only dependency pinned by Home Assistant 2026.9.3; not shipped with the integration."

[[IgnoredVulns]]
id = "GHSA-m2h6-j472-rp4c"
reason = "Test-only dependency pinned by Home Assistant 2026.9.3; not shipped with the integration."
